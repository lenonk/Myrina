<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:MyrinaUI.ViewModels;assembly=MyrinaUI"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="100" Name="LogView"
             x:Class="MyrinaUI.Views.LogView">
  
  <UserControl.DataContext>
    <vm:LogViewModel />
  </UserControl.DataContext>

  <Border Margin="0,4,4,0" BorderBrush="#23404C" BorderThickness="1">
    <Grid RowDefinitions="auto,*">
      <Grid Grid.Row="0" ColumnDefinitions="auto,*,auto" >
        <TextBlock VerticalAlignment="Center" Grid.Column="0" Margin="2,0,5,3" FontSize="11" Text="Activity Log:" />
        <Button VerticalContentAlignment="Center"
                Name="CollapseButton"
                MaxHeight="15" 
                Margin="5,5,5,5" 
                Grid.Column="2">v</Button>
      </Grid>
      <ListBox Height="100" 
               AutoScrollToSelectedItem="True" 
               Margin="-1,0,-1,-1" 
               Name="LogListBox" 
               Grid.Row="1" 
               BorderBrush="#23404C" 
               Items="{Binding LogItems}" />
    </Grid>
  </Border>
</UserControl>
